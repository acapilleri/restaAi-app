/**
 * @deprecated Token chat sono in AppColors; usare useTheme().colors (chatLine, chatScreenBg, …).
 * Re-export da lightColors solo per compatibilità temporanea.
 */
import { lightColors } from './colors';

export const CHAT_LINE = lightColors.chatLine;
export const CHAT_SCREEN_BG = lightColors.chatScreenBg;
export const CHAT_BUBBLE_BORDER = lightColors.chatBubbleBorder;
export const CHAT_SHELL_BG = lightColors.chatShellBg;
export const CHAT_MUTED = lightColors.chatMuted;
