/**
 * @deprecated Avvio sincrono inaffidabile: l'init avviene da SensorFusionProvider (ensureExecutorchInit).
 * File mantenuto per compatibilità se qualcosa importa ancora questo path.
 */
export { ensureExecutorchInitialized, hasETInstallerModule } from './ensureExecutorchInit';
